# User-owned Superpowers patches

These files replace the named user-owned Skill files under `<CODEX_HOME>/skills/`. They were prepared for the local hashes recorded in `SKILL-AUDIT.md`. Back up a changed upstream version and re-run `skill-intake-auditor` before replacing it.
